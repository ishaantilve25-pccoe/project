# 2D Function Maxima and Minima Finder
A Python script using SymPy to find and classify stationary points of a 2-variable function.

## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the script: `python main.py`
